"use client";


// Fallback for cn if it doesn't exist (I'll define a simple one inline if needed to be safe, but usually projects have one)
// I'll assume standard Tailwind classes usage directly if I'm unsure about `lib/utils`. 
// Actually I'll verify if `lib/utils.ts` exists.
// Looking at the file list, `lib` exists. I'll check inside `lib`.
